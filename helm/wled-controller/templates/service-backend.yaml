apiVersion: v1
kind: Service
metadata:
  name: {{ include "wled-controller.fullname" . }}-backend
  labels:
    {{- include "wled-controller.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "wled-controller.backendSelectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 8080
      name: http
